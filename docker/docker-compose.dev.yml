services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: banglab2bb2c:local
    container_name: banglab2bb2c_app
    restart: unless-stopped
    ports:
      - "8081:8080"
    networks:
      - banglab2bb2c
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads:rw
      - ./config.toml:/app/config.toml:ro
    command: ["./banglab2bb2c", "server", "-migrate", "-config", "/app/config.toml"]

  db:
    image: postgres:17-alpine
    container_name: banglab2bb2c_db
    restart: unless-stopped
    networks:
      - banglab2bb2c
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-banglab2bb2c}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-banglab2bb2c}
      POSTGRES_DB: ${POSTGRES_DB:-banglab2bb2c}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-banglab2bb2c} -d ${POSTGRES_DB:-banglab2bb2c}"]
      interval: 10s
      timeout: 5s
      retries: 6
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: banglab2bb2c_redis
    restart: unless-stopped
    networks:
      - banglab2bb2c
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - redis-data:/data

networks:
  banglab2bb2c:

volumes:
  postgres-data:
  redis-data:
